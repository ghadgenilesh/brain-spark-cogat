[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    unit: Pure Python unit tests (no browser, no server)
    e2e: End-to-end Playwright browser tests (requires playwright install)
    slow: Tests that take more than a few seconds
